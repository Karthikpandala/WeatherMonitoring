<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Result</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(to right, #7ca6d0, #d8dbde);
            color: #000000;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
            max-width: 400px;
            width: 100%;
        }

        h1 {
            font-size: 2em;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2em;
            margin: 10px 0;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            color: #40b0dd;
            background-color: white;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #e0e0e0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Weather Information for {{weather.city}}</h1>
        {{#if weather.error}}
            <p>{{weather.error}}</p>
        {{else}}
            <p>Temperature: {{weather.temperature}}</p>
            <p>Feels Like: {{weather.feels_like}}</p>
            <p>Condition: {{weather.condition}}</p>
            <p>Data retrieved at: {{weather.time}}</p>
        {{/if}}
        <a href="/">Go Back</a>
    </div>
</body>
</html>
